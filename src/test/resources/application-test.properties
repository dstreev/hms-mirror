# RocksDB Test Configuration
hms-mirror.rocksdb.enabled=true
hms-mirror.rocksdb.test-mode=true
hms-mirror.rocksdb.path=${java.io.tmpdir}/hms-mirror-test-${random.uuid}
hms-mirror.rocksdb.aggressive-test-cleanup=true

# Disable other services that might interfere with testing
server.port=0
